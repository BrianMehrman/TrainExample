<%= form_for(@train, html:{class:"form-horizontal"}) do |f| %>
  <% if @train.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@train.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
      <% @train.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :train_line, class:"col-md-2 control-label" %>
    <div class="col-md-10"><%= f.text_field :train_line %></div>
  </div>
  <div class="form-group">
    <%= f.label :route_name, class:"col-md-2 control-label" %>
    <div class="col-md-10"><%= f.text_field :route_name %></div>
  </div>
  <div class="form-group">
    <%= f.label :run_number, class:"col-md-2 control-label" %>
    <div class="col-md-10"><%= f.text_field :run_number %></div>
  </div>
  <div class="form-group">
    <%= f.label :operator_id, class:"col-md-2 control-label" %>
    <div class="col-md-10"><%= f.text_field :operator_id %></div>
  </div>
  <div class="form-group actions">
    <div class="col-md-2 col-md-offset-2"><%= f.submit %></div>
  </div>
<% end %>
